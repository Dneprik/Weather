<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:weatherR="clr-namespace:WeatherR;assembly=WeatherR"
             x:Class="WeatherR.Views.MainPage">
    <StackLayout VerticalOptions="Center">
        <Label Text="{Binding City}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding TemperatureToday}" VerticalOptions="Center" HorizontalOptions="Center" />

        <weatherR:BindablePicker ItemsSource="{Binding Cities}" SelectedIndex="{Binding SelectedCityIndex}">
            

        </weatherR:BindablePicker>
    </StackLayout>
</ContentPage>